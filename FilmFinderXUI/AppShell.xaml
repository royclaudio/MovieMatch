<?xml version="1.0" encoding="UTF-8" ?>


<Shell
        x:Class="FilmFinderXUI.AppShell"
        xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
        xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
        xmlns:local="clr-namespace:FilmFinderXUI"
        Shell.NavBarIsVisible="False"
        FlyoutHeaderBehavior="Fixed">


    <TabBar>
        <!--<Tab Title="Home"
             Icon="home.png" >
            <ShellContent Title="Layout A"
                          Shell.BackgroundColor="Blue"
                          ContentTemplate="{DataTemplate local:MainPage}" />
            <ShellContent Title="Layout B"
                          Shell.BackgroundColor="Blue"
                          ContentTemplate="{DataTemplate local:LikeUI}" />
        </Tab>-->
        <ShellContent Title="Home"
                      Icon="home.png"
                      ContentTemplate="{DataTemplate local:MainPage}" />
            <ShellContent Title="Search"
                      Icon="search.png"
                      ContentTemplate="{DataTemplate local:Search}" />
        
            <ShellContent Title="Recommended"
                      Icon="quality.png"
                      ContentTemplate="{DataTemplate local:Recommended}" />
      
            <ShellContent Title="Profile"
                      Icon="user.png"
                      ContentTemplate="{DataTemplate local:Profile}" />
    </TabBar>

</Shell>


